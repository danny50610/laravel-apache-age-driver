<?php

namespace Danny50610\LaravelApacheAgeDriver\Services;

use Illuminate\Support\Facades\DB;

class ApacheAgeService
{
    // public function createGraph(string $graphName)
    // {
        // return DB::connection($this->connectionName)
        //     ->select("SELECT * FROM ag_catalog.create_graph(?);", [$graphName]);
    // }

    // public function dropGraph(string $graphName, bool $cascade)
    // {
        // return DB::connection($this->connectionName
        //     )->select("SELECT * FROM ag_catalog.drop_graph(?, ?);", [$graphName, $cascade]);
    // }
}
